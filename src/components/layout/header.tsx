export default function Header() {
  return (
    <header className="main-header flex items-center justify-center m-3">
      <div className="max-w-[var(--content-max-width)] w-full">
        <h1 className="text-xl font-semibold text-white">Lagebild Dashboard</h1>
      </div>
    </header>
  );
}
