export default function Header() {
  return (
    <header className="main-header flex items-center justify-center mt-4 mb-8">
      <div className="max-w-[var(--content-max-width)] w-full">
        <div className="text-2xl font-semibold text-white">Lagebild Dashboard</div>
      </div>
    </header>
  );
}
